# Step 2 Prompt 修复静态验证

本记录只使用上一轮配置与原始输出重放程序，不包含新的 AI 调用，不计入 `calls.json` 的调用统计。

12 个阶段输出均通过当前程序解析与校验，静态重放为 12/12。

| 阶段 | 3 个案例修复前字符 | 修复后字符 | 变化 |
| --- | ---: | ---: | ---: |
| 需求对齐 | 11,328 | 10,485 | -7.4% |
| 背景判断 | 5,897 | 4,074 | -30.9% |
| 方向生成 | 11,235 | 11,235 | 0% |
| 大纲生成 | 19,875 | 17,724 | -10.8% |

说明：方向 Prompt 的固定规则未为追求数字而继续删除；它们分别约束方向差异、因果完整、课堂人物、忠实边界和角色协议。下一轮通过前置澄清缩小背景资料后，方向输入中的资料上下文预计会自然下降。大纲只保留已选方向相关资料；小马案例从 7,063 降至 6,255 字符（-11.4%），二战案例从 6,863 降至 5,520（-19.6%）。四人英雄案例无外部资料，故大纲长度不变；修复后的正常流程应在需求对齐后进入主线理解卡，不再生成三个方向。
