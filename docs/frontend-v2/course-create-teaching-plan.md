# 课程创建阶段三：教学规划

## 文档状态

- 状态：2026-08-07 新契约草案，待用户确认后实现。
- 本文档替代此前“从零配置难度、词数和知识点”的 Step 3 契约。
- 用户可见名称继续使用“教学规划”。

## 模块目标

Step 3 将 Step 1 的教学目标、Step 2 的章节知识点推荐和系统固定默认值组合成一份完整、可编辑、可确认的教学规划。

老师进入页面时不再从空白状态逐项配置。页面默认已经包含：

- Step 1 选择的英语难度。
- 系统按难度、课程时长和章节数计算的每章推荐词数。
- Step 2 AI 为每章推荐的知识点和简短理由。
- 正文默认无题目。
- 每章默认 5 道选择题和 5 道填空题。
- 课后练习待老师明确确认；选择生成后，默认带入 5 道选择题和 5 道填空题。

Step 3 仍是配置页。老师可以看到并逐章修改开放的配置，但主要任务从“录入全部配置”变为“检查完整方案并修改例外”。

本阶段不调用 AI，不生成正文、真实题目、答案、解析、图片 prompt 或课件页面。

## 输入与真相源

必须读取：

- `Course.durationMinutes`。
- `Course.englishLevel`，来自 Step 1，只读。
- `Course.knowledgePointIds`，来自 Step 1，是 AI 生成章节推荐时的教学目标集合。
- 已确认的故事大纲及章节顺序。
- 每章 `recommendedKnowledgePointIds` 和 `knowledgePointRecommendationSummary`，来自 Step 2。
- 当前启用的知识点库，用于展示名称和支持手动调整。

边界：

- Step 3 不允许修改英语难度。需要修改时返回 Step 1，并按下游重置规则重新生成故事大纲。
- Step 3 每章知识点可以从当前启用的完整语法库中增删。Step 1 只约束 AI 推荐范围，不限制老师在配置阶段补充知识点。
- Step 2 推荐是 Step 3 初始值，不是不可修改的最终结果。
- Step 2 大纲中的推荐知识点和原因是不可编辑的 AI 基线；Step 3 不复制或改写该基线，只保存老师最终选择。
- Step 3 确认后的教学规划是“文案与练习”阶段唯一读取的执行版本。

## 词数计算

词数不由 AI 输出。系统先确定全课总词数，再平均分配到各章。

| 难度 | 30 分钟 | 45 分钟 | 60 分钟 |
| --- | ---: | ---: | ---: |
| A1-A2 | 200 词 | 300 词 | 400 词 |
| B1-B2 | 240 词 | 360 词 | 480 词 |
| C1-C2 | 280 词 | 420 词 | 560 词 |

```text
每章推荐词数 = 全课总词数 ÷ 章节数，再四舍五入到 10 的倍数
```

规则：

- 初始规划中所有章节使用相同推荐词数。
- 老师可以在 Step 3 将单章目标词数改为 50-200。
- 修改单章后不自动调整其他章节，也不要求总词数继续严格等于初始预算。
- 提供“同步正文设置到全部章节”，老师可以主动统一词数和正文模式。
- 公式集中在服务端领域函数中，Step 2 展示和 Step 3 初始化复用同一函数，前端不复制计算规则。

## 固定练习默认值

不引入习题模板、模板选择器或 `templateId`。

服务端使用单一领域常量初始化每章：

```ts
const DEFAULT_CHAPTER_EXERCISE_POLICY = {
  readingExerciseMode: "none",
  embeddedExercises: {
    enabled: false,
    countsByType: { choice: 0, blank: 0, vocab: 0 },
  },
  chapterPractice: {
    enabled: true,
    countsByType: { choice: 5, blank: 5, vocab: 0, matching: 0 },
  },
} as const;
```

含义：

- 正文默认展示完整阅读，不插入题目。
- 每章默认生成 5 道选择题和 5 道填空题。
- 这 10 题属于题库生成量，不代表课堂必须全部完成。
- 老师可以在 Step 3 修改单章题型、数量和开启状态。
- 手动添加任一新题型时默认添加 5 题。
- 所有题型格式示例统一使用“举例：”前缀，避免老师把示例误认为本课程已经生成的真实题目。
- 下游生成读取已确认教学规划中的实际配置，不再次套用默认常量。

## 页面结构

桌面端继续使用左侧章节概要、右侧配置区和底部确认操作。

### 顶部全课摘要

只读展示：

```text
B1 · 45 分钟 · 4 章 · 全文约 360 词
全课知识点 6 个 · 正文默认纯阅读 · 每章默认 10 题
```

顶部不提供难度选择器。难度旁提供“返回基础信息修改”入口，但修改会触发既有下游重置确认。

如果 Step 2 有未纳入推荐的知识点，顶部展示短提示和知识点名称：

```text
AI 已结合故事内容、难度和课时完成匹配。未进入章节推荐的知识点仍可在本页添加。
```

### 左侧章节概要

每章两行展示最终配置：

```text
第 2 章 · 蓝色书页
90 词 · 2 个知识点
纯正文 · 章节练习 10 题
```

章节完整时显示完成状态；异常时显示具体短提示，不使用只显示数量的模糊标签。

### 右侧章节配置

每章展示：

- 章节标题和故事摘要。
- 目标词数。
- 当前知识点。
- Step 2 的一句推荐理由，使用次要层级展示。
- 正文模式。
- 章节练习。

知识点行为：

- 初始值是 Step 2 推荐结果。
- “调整知识点”展示当前启用的完整语法库，并支持按类别筛选。
- 当前选择中，属于 Step 2 基线的项目标记“AI 推荐”，老师后来增加的项目标记“手动添加”。
- 当当前选择与 Step 2 基线不一致时，展示“重置为 AI 推荐”；重置只恢复本章知识点，不修改词数或练习。
- 每章至少保留 1 个知识点。
- 单章超过 3 个时提示：`本章知识点较多，建议确认是否能充分练习。`
- 老师修改后标记为手动配置，不再被重新打开页面时的推荐值覆盖。

正文行为：

- 默认“无题目”，并说明：`将展示完整正文，不在阅读中插入题目。`
- 可切换为“加入内嵌题”，支持选择题、填空题和词汇题。
- 匹配题不能作为正文内嵌题。
- “同步正文设置到全部章节”只同步词数、正文模式和内嵌题配置，不覆盖知识点。

章节练习行为：

- 默认开启，初始为选择题 5 道、填空题 5 道。
- 支持选择题、填空题、词汇题和匹配题。
- 开启/关闭控件必须同时通过文字与明显状态区分。
- “同步章节练习到全部章节”只同步章节练习开启状态、题型和题量。

### 课后练习

- 初始为待确认，不把默认关闭伪装成老师已经选择。
- 老师必须明确选择“生成课后练习”或“不生成课后练习”后才能确认教学规划。
- 课后练习知识点只能来自 Step 1 的全课知识点。
- 老师选择“生成课后练习”时，默认带入选择题 5 道、填空题 5 道，之后可以修改。
- 课后练习知识点默认是所有章节当前所选知识点的去重汇总；老师未手动调整课后知识点前，章节修改会持续同步该汇总。
- 选择生成课后练习后，汇总出的知识点全部以已勾选状态展示；老师通过取消勾选排除不需要考查的内容，不需要重新逐项选择。

## 初始化与手动修改

首次进入 Step 3 且不存在教学规划时，服务端一次性创建完整草稿：

1. 从 Step 1 读取难度和全课知识点。
2. 使用统一公式计算每章词数。
3. 以 Step 2 每章推荐知识点初始化老师的章节选择；AI 推荐基线继续从 Step 2 读取。
4. 使用固定领域常量初始化正文和章节练习。
5. 将课后练习置为待确认。

初始化只执行一次。之后重新进入页面必须读取已保存草稿，不能重新计算并覆盖老师修改。

字段继续使用 `touched` 标记手动修改：

- 单章词数。
- 单章知识点。
- 正文模式和内嵌题。
- 章节练习。
- 课后练习决定及配置。

## 自动保存与确认

修改后自动保存草稿：

- 页面显示未保存、保存中、已保存和保存失败。
- 保存失败时保留所有本地输入。
- 自动保存不推进阶段，也不重置下游。

确认时必须校验：

- Step 1 难度和全课知识点仍存在。
- 故事大纲已确认，章节 ID 和顺序一致。
- 每章词数在 50-200。
- 每章至少 1 个知识点，且全部属于 Step 1 全课知识点。
- 开启的内嵌题和章节练习至少有 1 种题型且题量有效。
- 课后练习已经明确选择。
- 如果生成课后练习，其知识点和题量配置完整。

校验失败时定位到第一个问题所在章节或课后练习，不推进课程阶段。

## API 合同

路径保持不变：

- `GET /api/courses/:id/teaching-plan`
- `PUT /api/courses/:id/teaching-plan`
- `POST /api/courses/:id/teaching-plan/confirm`

`GET` 在不存在规划时按本契约创建完整草稿，不再创建空壳子。

`PUT` 保存老师修改后的规划，不允许修改 Step 1 难度；章节知识点必须来自当前启用的完整语法库。

`POST .../confirm` 校验并确认规划。如果已存在文案、练习或视觉资源，继续使用 `409 + resetDownstream` 二次确认和后端事务重置。事务失败时旧确认状态和旧下游内容保持不变。

## 数据结构

```ts
type TeachingPlan = {
  courseId: string;
  status: "draft" | "confirmed";
  chapters: TeachingPlanChapter[];
  afterClassPractice: AfterClassPracticeConfig;
  updatedAt: string;
  confirmedAt: string | null;
};

type TeachingPlanChapter = {
  outlineChapterId: string;
  targetWordCount: number;
  knowledgePointIds: string[];
  readingExerciseMode: "none" | "embedded";
  embeddedExercises: ExerciseConfig;
  chapterPractice: PracticeConfig;
  touched: {
    targetWordCount: boolean;
    knowledgePointIds: boolean;
    readingExerciseMode: boolean;
    embeddedExercises: boolean;
    chapterPractice: boolean;
  };
};
```

`englishLevel` 不再作为 `CourseTeachingPlan` 的独立可写字段；API 可以在页面状态中从 `Course` 派生返回，但不得形成第二真相源。

## 失败恢复与下游边界

- Step 1 修改难度或全课知识点后，必须重新生成 Step 2，大纲推荐和 Step 3 规划一起重置。
- Step 2 重新生成或修改确认后的大纲时，必须重置 Step 3 及后续内容。
- Step 3 自动保存失败不清空输入、不推进阶段。
- Step 3 修改已确认规划时，确认操作才触发下游重置保护。
- 文案与练习阶段必须严格读取确认后的 Step 3 配置，不使用 Step 2 推荐重新推导。
- 本阶段不生成真实题目；固定默认值只决定后续需要生成的题型和数量。

## 验收标准

- Step 3 首次进入时已经是一份完整可检查的方案，不要求老师从零选择难度、词数、知识点和章节练习。
- 难度只读来自 Step 1。
- 每章词数由统一公式初始化，并允许单章修改。
- 每章知识点默认来自 Step 2，老师可从完整语法库调整，并能一键重置为 AI 推荐。
- AI 推荐知识点与手动添加知识点有明确标记；推荐原因刷新和自动保存后仍存在。
- 课后练习知识点默认汇总各章当前选择，未手动调整前随章节选择同步。
- 未被 AI 纳入推荐的知识点有明确但非阻断提示。
- 正文默认无题目。
- 每章默认 5 道选择题和 5 道填空题。
- 选择生成课后练习时，默认 5 道选择题和 5 道填空题。
- 不出现习题模板名称或模板选择器。
- 老师可以修改单章配置，并可以分别同步正文设置和章节练习到全部章节。
- 课后练习必须明确选择后才能确认。
- 自动保存、确认和下游重置失败都不丢失旧结果或本地输入。

## 实现状态

- 状态：已实现。Step 3 使用 Step 1 难度、Step 2 AI 推荐基线和系统固定练习默认值；老师可从完整语法库修改，并可重置单章 AI 推荐。
- 验证命令：全量 144 项测试通过；`pnpm lint`、`pnpm build` 通过。
- 提交号：待本次用户验收后记录。
