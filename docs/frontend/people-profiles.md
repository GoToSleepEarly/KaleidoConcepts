# 人物档案模块说明

## 模块目标

将原学生列表升级为人物档案，用于维护教案生成和图片生成会引用的人物基础信息。

本期支持两类人物：
- 教师
- 学生

## 命名决策

模块名使用 `人物档案`。

原因：
- 当前需求本质不是组织人员管理，而是 AI 生成内容中的人物素材库。
- 教师和学生都可能出现在教案、绘本和插图里。
- 后续如增加助教、家长或故事角色，可以继续扩展 role。

## 页面入口

- 路由：`/people`
- AppShell 导航：`人物档案`
- AppShell header 标题：`人物档案`

本期不保留 `/students` 旧路由。

原因：
- 当前产品未上线，不需要兼容旧入口。
- 保留旧路由会让模块命名继续混乱。

## 本期范围

包含：
- 人物档案列表
- 教师 / 学生 role 过滤
- 顶部 Tab 仅保留学生和老师
- 新增人物
- 编辑人物
- 右侧抽屉表单
- 空状态
- 数据库持久化
- `GET /api/people`
- `POST /api/people`
- `PUT /api/people/:id`

不包含：
- 删除
- 归档
- 头像上传
- 教师登录账号绑定
- 多教师权限
- 课程创建流程改造
- 教师人物在 prompt 中的实际使用

## 数据模型

使用统一 `Person` 表，而不是 `TeacherProfile` + `Student` 两张表。

原因：
- 本期教师和学生都是 AI 生成内容中的人物档案。
- 课程生成后续需要统一读取 teacher + students。
- 页面也是一个统一的资料库，只按 role 过滤。

```ts
type PersonRole = "teacher" | "student";

type PersonProfile = {
  id: string;
  role: PersonRole;
  name: string;
  chineseName?: string;
  englishName?: string;
  age?: number;
  gender?: "male" | "female";
  appearance?: string;
  interests: string[];
  learningGoal?: string;
  notes?: string;
  avatarUrl?: string;
  createdAt: string;
  updatedAt: string;
};
```

## 字段规则

### 教师

必填：
- 名称

非必填：
- 性别
- 外貌描述
- 备注

### 学生

必填：
- 中文名
- 英文名
- 年龄
- 性别
- 外貌描述

非必填：
- 兴趣爱好
- 学习目标
- 备注

## 页面结构

顶部：
- 学生 / 老师 Tab、搜索和新增入口组成同一工具区。
- 页面正文不重复 AppShell 标题说明。

过滤：
- `学生`
- `老师`

列表：
- 使用卡片网格
- 不使用横向列表行。
- 1024 px 横屏默认 2 列，宽屏默认 3 列；卡片保持统一信息顺序和操作位置。
- 卡片顶部展示头像、中英文名、年龄与性别；正文只展示已填写的备注，底部固定编辑入口。
- 页面标题下方说明文案统一为 `人物资料会用于课程内容和插图生成。`，只解释数据用途，不重复操作说明。

## 分页与搜索（2026-08-13）

- `GET /api/people` 支持 `page` 与 `pageSize`，人物档案页默认每页 6 人。
- 分页由数据库执行 `skip/take`，同时返回 `page`、`pageSize`、`total`、`totalPages`；不得先读取全部人物再由前端切片。
- 搜索、人物类型切换后回到第 1 页；新增或编辑成功后重新查询当前页。
- 课程创建中的人物选择器继续允许请求最多 100 人，不展示管理页分页。

## 年龄校验（2026-08-13）

- 年龄允许 0–99 岁，必须为整数。
- 表单输入属性、前端提交校验与服务端 Zod 校验使用同一范围。
- 不修改数据库字段，不需要 migration。

## 新增 / 编辑抽屉

抽屉标题：
- 新增人物
- 编辑人物

新增时可以选择类型：
- 教师
- 学生

编辑时类型不可修改。

原因：
- role 变更会影响字段语义。
- 如果把学生改成教师，课程历史引用会变得难解释。

## API 合同

### `GET /api/people`

可选 query：

```text
role=teacher | student
page=1
pageSize=6
```

响应：

```ts
{
  people: PersonProfile[];
  page: number;
  pageSize: number;
  total: number;
  totalPages: number;
}
```

失败：
- `500 { message: "人物档案加载失败" }`

### `POST /api/people`

请求：

```ts
PersonInput
```

响应：

```ts
201 { person: PersonProfile }
```

失败：
- `400 { message: "人物信息不完整" }`
- `500 { message: "人物保存失败" }`

### `PUT /api/people/:id`

请求：

```ts
PersonInput
```

响应：

```ts
{ person: PersonProfile }
```

失败：
- `400 { message: "人物信息不完整" }`
- `404 { message: "人物不存在" }`
- `500 { message: "人物保存失败" }`

## 验收标准

- `/people` 显示人物档案页面
- AppShell 导航显示 `人物档案`
- 本期不保留 `/students`
- 页面顶部 Tab 仅显示学生 / 老师，默认进入学生
- 可新增教师，字段包含名称、性别、外貌描述、备注
- 教师未上传头像时按性别使用默认教师头像，男老师使用男老师默认头像
- 可新增学生，字段包含中文名、英文名、年龄、性别、外貌描述、兴趣、学习目标、备注
- 可编辑教师和学生
- 编辑时不能修改人物类型
- 数据写入真实数据库
- 学生旧数据迁移到 `Person(role=student)`
- seed 中包含至少 1 个教师和 2 个学生
- `pnpm test`、`pnpm lint`、`pnpm build` 通过

## 实现记录

- 状态：已实现，待用户验收
- 实现提交：待记录
- 实现内容：
  - `/people` 人物档案页面
  - `GET /api/people`
  - `POST /api/people`
  - `PUT /api/people/:id`
  - Prisma `Person` / `CoursePerson` migration
  - seed 默认教师档案和按性别区分的默认教师头像
- 验证命令：
  - `pnpm test`
  - `pnpm lint`
  - `pnpm build`
- 验证结果：
  - test 通过，5 个测试文件 / 10 个测试
  - lint 通过
  - build 通过
  - 本地数据库已迁移，旧 Student 数据已迁移为 `Person(role=student)`
- 2026-08-13：人物档案管理页改为响应式卡片网格，默认每页 6 人；搜索与分页由数据库执行；年龄校验统一为 0–99；AppShell 说明调整为人物资料的实际用途。
- 2026-08-13：视觉色彩对齐课程列表蓝色体系；真实人物形象使用直角缩略图并保留性别标记，默认头像继续使用圆形；分页支持直接选择页码；空备注不再占用正文高度。
- 2026-08-13：人物形象统一采用与生成服务一致的 2:3 竖版画幅。生成文件固定为 1024×1536；档案卡片使用 72×108 px，版本缩略图使用 56×84 px，调整记录使用 48×72 px；真实人物图统一 `object-contain`，禁止裁剪覆盖。
- 2026-08-13：移除真实人物图外层白底与白色描边，卡片重构为“左侧固定 2:3 人物图 / 右侧身份信息 / 底部编辑与删除操作”；卡片不再展示备注。删除使用项目内确认弹窗并调用归档接口。
- 2026-08-13：档案卡片人物缩略图放大为 96×144 px，点击真实人物图可直接打开 2:3 大图预览；默认头像不可点击。人物存在时始终展示分页栏，单页状态保留总数、页码选择器和禁用的前后翻页按钮。
- 2026-08-13：真实人物图与默认圆形头像的性别标记统一固定在右下角；“查看大图”提示移到图片顶部，避免遮挡。
- 2026-08-13：人物形象工作台统一使用 2:3 主预览；历史版本可直接查看、设为当前或删除。当前使用、被后续版本依赖或被课程引用的版本禁止删除；数据库记录删除后同步清理本地图片。重新创建移到预览区全局操作，聊天区只负责基于选中版本继续修改；修改提示示例改为“头发改成红色”。
- 2026-08-13：人物形象编辑使用固定预览栏 + 自适应操作栏。预览画布本身固定 2:3，不再使用横向外框；主图支持点击查看大图。创建、生成、修改和历史版本切换均保持外层工作台尺寸不变，仅在右栏内部滚动，避免界面跳变。
- 2026-08-13：人物编辑弹窗统一改为居中的 900 px 中型规格，人物形象页左右两栏按 1:1 平分；基础资料与人物形象切换时保持同一尺寸和位置。
- 2026-08-13：形象采用操作按来源区分：主动选择历史版本时在左侧显示“设为当前形象”；聊天刚生成的新版本在右侧结果卡片显示“使用这个新形象”，左侧仅标记“新生成”。点击重新创建后按钮改为“取消重新创建”，取消时恢复此前选中的聊天修改界面。
- 2026-08-13：人物形象继续修改时，Prompt 继续显式携带人物年龄与性别，并要求保持年龄感和性别特征，降低多轮修改后的形象漂移。
